# Local dev: leave unset or empty to use Vite proxy → http://127.0.0.1:8000
# VITE_API_URL=

# Production: either set BACKEND_URL on Vercel (see README) for /api proxy, OR bake the API URL at build:
# VITE_API_URL=https://your-backend-name.onrender.com
